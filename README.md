# Opening Day Ticket Sales Changes Report

An analysis of how strategic pricing changes for Opening Day affected ticket revenue, sales volume, site traffic, and sales pacing compared to prior seasons.

## Overview

This project examines the impact of intentionally reducing season ticket inventory and increasing single-game ticket prices for Opening Day. The analysis spans 4–5 seasons of data across 30+ seating price locations in a professional baseball stadium.

**Key questions addressed:**
- Did higher prices drive enough revenue growth to offset lower volume?
- How did sales pacing change when tickets went on sale later?
- Did site traffic and conversion rates hold up?
- Which price locations were most/least affected?

## Project Structure

```
├── analysis.R                      # Data generation, analysis, and all visualizations
├── TicketSalesChangesReport.Rmd    # R Markdown report (knits to PDF)
├── data/                           # CSVs generated by analysis.R
│   ├── ticket_group_table.csv
│   ├── avg_price_overall.csv
│   ├── avg_price_by_month.csv
│   ├── price_change.csv
│   ├── season_ticket_avg_price.csv
│   └── season_ticket_overall.csv
└── images/                         # Plots generated by analysis.R
```

## How to Run

1. Install required R packages:
   ```r
   install.packages(c("ggplot2", "dplyr", "tidyr", "lubridate",
                       "scales", "forcats", "knitr", "kableExtra"))
   ```

2. Run the analysis script to generate all data and visualizations:
   ```r
   source("analysis.R")
   ```

3. Knit the report to PDF:
   ```r
   rmarkdown::render("TicketSalesChangesReport.Rmd")
   ```

## Tools & Techniques

- **R** — data wrangling (dplyr, tidyr, lubridate), visualization (ggplot2), reporting (R Markdown, kableExtra)
- **SQL (BigQuery)** — original data was sourced from Google BigQuery (clickstream, ticketing, and event tables). The portfolio version uses synthetic data generated in R.
- **Analysis methods** — year-over-year comparison, price elasticity assessment, cumulative site traffic analysis, sales pacing snapshots, conversion rate tracking

## Data Note

All data in this repository is **synthetically generated** for portfolio purposes. The analytical framework, methodology, and visualization approach reflect a real project originally conducted using proprietary ticket sales data from an MLB franchise. Original data was sourced from BigQuery tables spanning clickstream/site traffic, Archtics ticketing transactions, and event metadata.
